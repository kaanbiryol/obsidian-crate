export {
	handleBatchDelete,
	handleBatchUpload,
	handleCheckChanges,
	handleDelete,
	handleDownload,
	handleGetChanges,
	handleGetFileMetadata,
	handleGetManifest,
	handleGetSettings,
	handleHealth,
	handlePutSettings,
	handleUpload,
} from '../sync-handlers';
